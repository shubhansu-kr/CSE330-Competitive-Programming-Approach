// Sieve of Atkin: The Sieve of Atkin is a modern algorithm for finding all prime
// numbers up to a specified integer. Compared with the ancient Sieve of Erastothenes
// 

// Create a results list, filled with 2, 3, 5.
// Create a sieve list with an entry for each positive integer:  all entries should be initially
// be marked non prime.
// For each entry number n in the sieve list, with modulo sixty remainder r: 
//      if r is 1, 13, 17, 29, 37, 41, 49 or 52, flip the entry for eaach possible
//      solution to 4x^2 + y^2 = n;
//      If r is 7, 19, 31 or 43, flip the entry for each possible solution to 3x^2+y^2 = n
//      if r is 11, 23, 47, or 59, flip the enry for each possible solution to 3x^2-y^2 <= n


#include <bits/stdc++.h>
using namespace std ;



int main () {
    
    return 0;
}