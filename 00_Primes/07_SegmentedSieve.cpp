// Segmented Sieve Algorithm: 

#include <bits/stdc++.h>
using namespace std ;

// Divide the range [0, n-1] in different segments.
// Size of each segment is root n.
// Compute primes in all segments one by one.

// TC: O(n*loglog(n)) 
// SC: O(root(n) + l)                           : l -> number of primes 

class Solution {
public: 
    vector<int> segmentedSieve(int n) {
        
    }
};

int main () {
    
    return 0;
}