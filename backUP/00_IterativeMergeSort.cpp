// Iterative merge sort

#include <bits/stdc++.h>
using namespace std ;

void merge(vector<int> &nums, int l1, int h1, int l2, int h2) {
    int size = (h1- l1) + (h2 - l2);

}
void mergeSort(vector<int> &nums) {
    for (int len = 1; len < nums.size()+1; ++len)
    {
        
    }
    
}

int main () {
    
    return 0;
}